.console/*
!.console/task.md
!.console/backlog.md
!.console/guidelines.md
!.console/log.md
# Reconciliation worksheet + local archive staging (console-reconciliation-spec.md).
/.console/reconcile.yaml
/.console/archive/
CLAUDE.md
.custodian/tmp*.yaml

__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
build/
dist/

# .context/ cognition state is machine-local (capsules, leases, checkpoints) —
# config/engine/knowledge/templates are tracked; live session state never is.
# (Guard: a bare `git add -A` once staged 76k lease files.)
.context/sessions/*/
.context/archived/*/

# Claude Code local runtime state (wakeup scheduling)
.claude/scheduled_tasks.lock
